run before production

if already running queue run this, if not ignore this.
    php artisan queue:restart

then run this
    php artisan queue:work

run this as a cron job, set up for 00.00 every day.

    php artisan app:daily-income-command

make sure to link storage

    php artisan storage:link
